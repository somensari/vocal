# Add project specific ProGuard rules here.
#
# New Relic Android agent (7.6.8+) ships its own keep rules via the Gradle plugin.
# If you enable minify for release and map upload, supply the application token via
# local/CI config only — never commit app/newrelic.properties with a real token.
# See docs/new-relic.md.
